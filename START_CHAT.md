Read SPEC.md and CLAUDE_RULES.md.

Milestone 1 (must end runnable):
- Create backend NestJS project inside /backend (TypeScript).
- Setup Prisma + connect to local Postgres from docker-compose.yml at repo root.
- Add Prisma schema (User, Category, CategoryTarget, TimeEntry, ReviewNote) based on SPEC.
- Run migrations.
- Ensure `npm run start:dev` works.
- Provide the exact commands I should run to verify.

Keep it minimal. No NextAuth. No monorepo. No shared packages.
All code + comments in English. Discussion in Vietnamese.